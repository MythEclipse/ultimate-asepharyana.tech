apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    # Container DNS — resolves within app-shared-net
    url: http://prometheus:9090
    access: proxy
    isDefault: true
    editable: false
    jsonData:
      httpMethod: POST
      scrapeInterval: 15s
      queryTimeout: 60s
