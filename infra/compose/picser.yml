services:
  picser:
    container_name: picser
    image: ghcr.io/mytheclipse/picser:latest
    restart: always
    networks:
      app-shared-net:
        aliases:
          - picser
    env_file:
      - .env.picser
networks:
  app-shared-net:
    name: app-shared-net
    external: true
