tls:
  certificates:
    - certFile: /etc/traefik/certs/asephstech.pem
      keyFile: /etc/traefik/certs/asephscloud.key
  stores:
    default:
      defaultCertificate:
        certFile: /etc/traefik/certs/asephstech.pem
        keyFile: /etc/traefik/certs/asephscloud.key
